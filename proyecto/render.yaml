service:
  type: web

  name: proyecto
  runtime: python
  plan: free
  region: oregon
  branch: main

  buildComand: bash build.sh

  startcommand: gunicorn proyecto.wsgi:application

  healthCheckPath: /